# Camera backends — see CLAUDE.md for policy.
#
# picamera==1.13: EXCLUDED from Docker build.
#   Its setup.py reads /proc/cpuinfo to detect Raspberry Pi hardware.
#   This check fails in any Docker/QEMU environment. Since armv7/Bullseye
#   uses picamera2 as the primary backend (auto-detected in camera.py),
#   the legacy picamera path is not bundled in this binary. Install picamera
#   manually on Buster hardware if needed.
#
# RPi.GPIO==0.7.0: EXCLUDED — linker bug with GCC 10+ (multiple definition
#   of symbols in headers). Upgraded to 0.7.1 which fixes -fno-common issue.
RPi.GPIO==0.7.1
protobuf==4.22.3
pyzbar==0.1.9
google-api-core==2.17.1
google-auth==2.26.1
google-cloud-storage==2.16.0
google-api-python-client==2.86.0
google==3.0.0
slack-sdk
requests
pika==1.3.1
# pycrypto is unmaintained since 2014 and has no Python 3.9 wheel.
# pycryptodome is a drop-in compatible fork.
pycryptodome
diskcache==5.6.1
# opencv-python: installed from source via cmake in the Dockerfile.
# numpy: installed via apt (python3-numpy) in the Dockerfile.
# Do not add either here.
