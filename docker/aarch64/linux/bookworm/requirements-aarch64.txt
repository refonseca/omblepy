# picamera2: system package — installed via apt in the Dockerfile (python3-picamera2).
# Do not add it here; it depends on libcamera C bindings not available on PyPI.
# picamera (legacy): not used on bookworm/aarch64. picamera2 is the backend here.
protobuf==4.22.3
pyzbar==0.1.9
# RPi.GPIO does not support Raspberry Pi 5 (RP1 chip).
# rpi-lgpio is a drop-in replacement with the same RPi.GPIO API, using lgpio underneath.
# Installed via apt (python3-rpi-lgpio from the RPi repo) in the Dockerfile.
# Do not add RPi.GPIO or rpi-lgpio here.
google-api-core==2.17.1
google-auth==2.26.1
google-cloud-storage==2.16.0
google-api-python-client==2.86.0
google==3.0.0
slack-sdk
requests
pika==1.3.1
# pycrypto==2.6.1 is unmaintained since 2014 and has no Python 3.11 wheel.
# Replace with pycryptodome, which is a drop-in compatible fork.
pycryptodome
diskcache==5.6.1
# opencv-python: installed via apt (python3-opencv) in the Dockerfile to avoid
# QEMU SIGSEGV during cmake source compilation. Do not add it here.
# numpy: installed via apt (python3-numpy) in the Dockerfile. Do not add it here.
